The dataset contains the preprocessed and annotated images .

The train set is used for training the model

The test set is used to test the model

The validate set is used to measure the performance of the model
